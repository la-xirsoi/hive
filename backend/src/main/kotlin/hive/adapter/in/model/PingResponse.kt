package hive.adapter.`in`.model

/**
 * Proves that a *nested* sub-package under the escaped `in` segment compiles and
 * can be imported from elsewhere.
 */
data class PingResponse(val status: String)
