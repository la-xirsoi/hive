package hive.domain

/**
 * Domain layer (Hexagonal Architecture core).
 *
 * Contents: entities, value objects and repository *interfaces* (ports).
 * This layer must not depend on Spring, JPA, or any adapter package.
 *
 * Placeholder file -- keeps the package directory in version control until the
 * first real domain type lands.
 */
internal const val LAYER: String = "domain"
