package hive.application

/**
 * Application layer (Hexagonal Architecture).
 *
 * Contents: use-case / service interfaces and their orchestration logic.
 * Depends on [hive.domain] only -- never on adapters.
 *
 * Placeholder file -- keeps the package directory in version control until the
 * first real use case lands.
 */
internal const val LAYER: String = "application"
